import app from './app';

export const articleService = app.service('articles');
export const categoryService = app.service('categories');
export const questionService = app.service('questions');
export const uploadsService = app.service('uploads');
